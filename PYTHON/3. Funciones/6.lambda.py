# EXPRESIONES LAMBDA => no tiene un nombre, se crea con la palabra reservada lambda, se usan para definir funciones pequeñas

# Crear la función lambda, y el resultado la guardamos en la variable resultado
def resultado(a, b): return a+b


# Mostrar el resultado
print(resultado(10, 5))
