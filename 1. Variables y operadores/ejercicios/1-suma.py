# Suma de números
print("Suma de números")

# Entrada de datos, lo convertimos a float, ya que el input por defecto devuelve un string, también se podría convertir a int
a = float(input("Primer número: "))
b = float(input("Segundo número: "))

# Operación
suma = a + b

# salida
print("La suma es: ", suma)
